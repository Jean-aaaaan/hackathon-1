# This directory holds Playwright auth state (user.json).
# user.json is gitignored — it's generated by auth.setup.ts and contains session tokens.
# .gitkeep ensures the directory is tracked so CI can write to it.
