# Dev requirements — Python 3.13 compatible
# hubspot-api-client excluded (Windows long-path issue, optional for local dev)
# psycopg2-binary excluded (asyncpg handles async; psycopg2 only needed for alembic sync mode)

# Core
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic>=2.10.3
pydantic-settings>=2.7.0
python-dotenv==1.0.1

# Database
sqlalchemy==2.0.36
alembic==1.14.0
asyncpg==0.30.0
pgvector==0.3.6

# Cache
redis==5.2.1
hiredis==3.1.0

# HTTP
httpx>=0.27.0
aiohttp>=3.11.0

# Auth
python-jose[cryptography]==3.3.0
# workos installed separately with --no-deps to avoid pydantic pin (dev bypass active)

# AI
anthropic>=0.40.0
voyageai>=0.3.3

# Integrations (azure optional — comment out if not needed)
azure-servicebus==7.14.2
azure-identity==1.19.0

# Utilities
pyyaml==6.0.2
structlog==24.4.0
sentry-sdk[fastapi]>=2.0
python-multipart==0.0.20
